apiVersion: v1
kind: Service
metadata:
  name: polar-order-postgres
  labels:
    db: polar-order-postgres
spec:
  type: ClusterIP
  selector:
    db: polar-order-postgres
  ports:
    - name: http
      protocol: TCP
      port: 5432
      targetPort: 5432